---
- hosts: chomsky
  user: root
  strategy: free

  vars:
    grupos: adm,cdrom,dialout,kvm,libvirt,lpadmin,lxd,plugdev,vboxusers

  roles:
    - delete-users
    - lock-sshd
    - swappiness-0
    - role: vasilisa
      vars:
        sirin: "http://192.168.23.18:8000/launcher/?label=chomsky-u2204.yml"
    - activate-unattended-upgrades
    - role: cambia-hostname
      vars:
        base_hostname: "chomsky"
    - timesyncd-ntp
    - ubuntu-desktop-minimal
    - software
    - firefox-cambia-snap-por-flatpak
    - gnome-dconf-2204
    - apache-userdir
    - wireshark
      #- dotnet
    - kvm
    - lxd-dir
    - glpi-agent
    - convidat
    - apagado-1455
    - apagado-2130

  tasks:
    # Usuarios
    #- include: users/esobat/docent.yml
    #- include: users/idiomes/idiomes.yml
    - include: users/informatica/alumnos/daw1.yml
    - include: users/informatica/adriamt.yml
      when: ansible_default_ipv4.address == "192.168.23.100"
    - include: users/informatica/fedejerez.yml
      when: ansible_default_ipv4.address == "192.168.23.100"
