---
- hosts: torvalds
  user: root

  tasks:
          - name: Borrado de alumnos
            user:
                    name: "{{ item }}"
                    state: absent
                    remove: true
            loop:
                    - smx1a
                    - asixdam1a
                    - isardvdi
